###############################################################################
# WEB TIER – VM CREATION
###############################################################################
- name: Web Tier - VM Creation
  hosts: localhost
  connection: local
  gather_facts: no

  vars:
    vm_flavor: "cmp"
    vm_image: "a997754d-6cdf-4a21-8e52-5df389d3f7c5"
    vm_keypair: "k8s"
    vm_net: "cb24a95d-378a-470b-9647-4849caca9fb2"

    os_auth_url: "http://192.168.170.51:5000"
    os_project_name: "Production Engineering"
    os_project_domain_id: "default"
    os_username: "admin"
    os_user_domain_name: "Default"
    os_password: "bLEZFvatBShkvUZyKrasUjmNdTTc9c3HcxWykrro"
    os_region_name: "RegionOne"
    os_interface: "internal"

  tasks:
    - debug:
        msg: "Creating Web VM with name {{ vm_name_web }}"

    - name: Create Web VM (boot from volume)
      openstack.cloud.server:
        auth:
          auth_url: "{{ os_auth_url }}"
          username: "{{ os_username }}"
          password: "{{ os_password }}"
          project_name: "{{ os_project_name }}"
          project_domain_id: "{{ os_project_domain_id }}"
          user_domain_name: "{{ os_user_domain_name }}"
        region_name: "{{ os_region_name }}"
        interface: "{{ os_interface }}"
        name: "{{ vm_name_web }}"
        flavor: "{{ vm_flavor }}"
        image: "{{ vm_image }}"
        key_name: "{{ vm_keypair }}"
        network: "{{ vm_net }}"
        boot_from_volume: true
        volume_size: 20
        terminate_volume: true
        wait: yes
        timeout: 600
      register: web_vm

    - add_host:
        name: "{{ web_vm.server.public_v4 }}"
        groups: web

###############################################################################
# WEB TIER – NGINX REVERSE PROXY
###############################################################################
- name: Web Tier - Nginx Setup
  hosts: web
  become: yes
  gather_facts: yes

  vars:
    app_ip: "{{ hostvars['localhost']['app_ip'] }}"

  tasks:
    - apt:
        name: nginx
        state: present
        update_cache: yes

    - copy:
        dest: /etc/nginx/sites-available/default
        content: |
          server {
              listen 80;
              location / {
                  proxy_pass http://{{ app_ip }}:8080;
              }
          }

    - service:
        name: nginx
        state: restarted
        enabled: yes
